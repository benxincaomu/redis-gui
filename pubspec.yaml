name: redis_gui_manager
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.2
  redis: ^3.1.0
  # sqlite3: ^1.11.0
  # sqlite3_flutter_libs: ^0.5.13
  sqflite: ^2.2.8
  xterm: ^3.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
